include/deal.II-translator/dofs/dof_objects_0.txt
[0.x.0]*
     存储位于维度为[2.x.0][1.x.0]的对象上的自由度的索引。DoFObjects类存储了某一层面上每个单元的自由度的全局索引。一个自由度的全局索引或数字是解向量中相应值的零基索引，以及该层的全局矩阵或多网格矩阵的行和列索引。这些指数指的是无约束的向量和矩阵，其中我们没有考虑到悬挂节点引入的约束。        由于顶点不与某一特定层次相关联，与顶点相关的指数不存储在DoFObjects类中，而是存储在[2.x.1]数组中。        DoFObjects类不被直接使用，但这些类的对象被包含在DoFLevel和DoFFaces类中。       
* [2.x.2]     
* [0.x.1]*
       存储自由度的全局指数。     
* [0.x.2]*
       将位于编号为[2.x.4]的对象上的[2.x.3]自由度的全局索引设置为最后一个参数所给的值。参数[2.x.5]用于访问要用来计算存储该数据的位置的有限元。            第三个参数，[2.x.6]必须等于零。否则它是未使用的，但我们保留这个参数，以便我们可以为非hp-和hp-有限元方法使用相同的接口，实际上使hp-和非hp-类之间共享DoFAccessor类的层次结构成为可能。     
* [0.x.3]*
       返回位于编号为[2.x.8]的对象上的[2.x.7]自由度的全局索引。 [2.x.9]参数用于访问要用来计算该数据存储位置的有限元。            第三个参数，[2.x.10]必须等于零。否则它将不被使用，但我们保留这个参数，以便我们可以为非hp-和hp-有限元方法使用相同的接口，实际上使hp-和非hp-类之间共享DoFAccessor类的层次结构成为可能。     
* [0.x.4]*
       返回值为1。这个函数的含义通过查看类[2.x.11]中的相应函数的内容而变得清晰。      
* [0.x.5]*
       与上面的函数类似。断言给定的索引是零，然后返回真。     
* [0.x.6]*
       确定这个对象的内存消耗（以字节为单位）的估计值。     
* [0.x.7]*
       使用[BOOST序列化库](https://www.boost.org/doc/libs/1_74_0/libs/serialization/doc/index.html)将此对象的数据读入或写入一个流中，以便进行序列化。     
* [0.x.8]

