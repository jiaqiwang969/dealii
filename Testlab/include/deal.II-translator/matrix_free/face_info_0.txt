include/deal.II-translator/Matrix_free/face_info_0.txt
[0.x.0]*
     数据类型，用于建立用于面积分矢量化的批次信息。面的批次的设置是独立于单元的，因此，我们必须存储与单元索引的关系，以便访问自由度。        内部面由两个相邻的单元存储，我们将其标记为面的 "内部 "和 "外部 "侧。存储在MappingInfo中的法线向量只存储一次，是 "内部 "面的单元的外侧法线，而 "外部 "面的符号则相反。        这个数据字段被存储为所有参与计算的面的一个向量。为了避免在内存表示中出现空隙，四个 "char "变量被放在彼此旁边，这与大多数架构上的无符号整数所占的大小相同。   
* [0.x.1]*
       当前面批中的面的指数，与面的逻辑 "内部 "侧的单元格的数字相比，该面的逻辑 "内部 "侧与[2.x.0]的方向对齐。      
* [0.x.2]*
       当前面批中的面的指数与面的逻辑 "外部 "侧的单元格数量相比，该面与[2.x.1]的方向相反。注意，内部和外部面的区分是纯逻辑的，仅指法线方向。在问题的实际离散化过程中，离散化通常需要确保内部和外部面得到正确的处理，例如上风通量。            对于边界面，数字被设置为[2.x.2]。      
* [0.x.3]*
       在面的 "外部 "的单元格内，0和[2.x.3]之间的面的索引。            对于一个边界面，这个数据字段存储了边界ID。     
* [0.x.4]*
       在面的 "内部 "一侧的单元格中，介于0和[2.x.4]之间的面的索引。     
* [0.x.5]*
       对于自适应细化网格，面的外侧的单元格可能比内侧的细化程度低。这个指数表示外侧的可能的子面指数。     
* [0.x.6]*
       在三维中，与一个面相邻的两个单元中的一个可能使用与标准方向不同的方向（也称为面朝向、面翻转和面旋转）。这个变量在第一个自由位中存储了本批面的面朝向、面翻转和面旋转（用于内部或外部的一个面）的值。如果内部单元有非标准方向，则第四位为一。           
* [2.x.5]与库中其他地方相比，面的方向位（第一位）是翻转的。     
* [0.x.7]*
       给定面的参考单元类型。0代表直线或四边形，1代表三角形。     
* [0.x.8]*
       返回当前数据结构的内存消耗。     
* [0.x.9]*
     保存面和单元之间的连接性的数据结构。   
* [0.x.10]*
       清除所有数据字段，使其处于类似于调用默认构造函数后的状态。     
* [0.x.11]*
       返回当前数据结构的内存消耗。     
* [0.x.12]*
       内部面的矢量存储，链接到矢量单元存储中的两个单元。     
* [0.x.13]*
       该表将宏观单元编号、单元内的面的索引和向量化的单元批内的索引三者转化为[2.x.6]数组内的索引。     
* [0.x.14]*
       使用与cell_and_face_to_plain_faces数据结构相同的索引，以矢量化格式存储面的边界ID      
* [0.x.15]

