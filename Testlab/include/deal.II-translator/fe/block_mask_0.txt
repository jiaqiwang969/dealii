include/deal.II-translator/fe/block_mask_0.txt
[0.x.0]*
 该类代表一个掩码，可用于选择有限元的单个矢量块（另见[2.x.0]"该术语条目"）。它通常有和有限元块一样多的元素，人们可以使用[2.x.1]来查询某个特定块是否被选中。
* 该类的语义与相关的ComponentMask类相同，即一个默认构造的掩码代表所有可能的块。关于这些语义的更多信息见那里。
* 这类对象在很多地方被使用，人们希望将操作限制在某个块的子集上，例如在[2.x.2]中，这些对象可以手工创建，或者更简单，要求有限元从某些选定的块中生成一个块掩码，使用这样的代码，我们创建一个掩码，只表示一个斯托克斯元的速度块（见[2.x.3] ）。

* 
* [1.x.0]
* 注意，通过将速度元素包装成一个FESystem对象，我们确保整个元素只有两个块。其结果是一个块掩码，在2D和3D中都有<code>[false, true]</code>的值。(将其与ComponentMask文档中讨论的相应组件掩码进行比较)。同样地，使用

* 
* [1.x.1]
* 将导致2d和3d中的掩码[2.x.4]。
* 

* 
* [2.x.5] 

* 
* [2.x.6] 

 
* [0.x.1]*
   初始化一个区块屏蔽。默认情况下，一个区块掩码代表一组被[1.x.2]选中的区块，也就是说，调用这个构造函数的结果是一个区块掩码，每当询问一个区块是否被选中时，总是返回[2.x.7]。 
* [0.x.2]*
   用参数指定的一组选定的块来初始化这个类型的对象。     [2.x.8] block_mask 一个[2.x.9]项的向量，决定有限元的哪些块被选择。如果给定向量的长度为零，则解释为[1.x.3]块被选中的情况。 
* [0.x.3]*
   用一些全部为真或假的元素来初始化块掩码。     [2.x.10] n_blocks 这个掩码的元素数量 [2.x.11] initializer 这些元素中的每一个应该具有的值：要么是真要么是假。 
* [0.x.4]*
   如果此块掩码已被初始化为大小大于0的掩码，那么返回此对象所代表的掩码的大小。另一方面，如果这个掩码已被初始化为一个空对象，代表一个对每个元素都是真的掩码（即，如果这个对象在调用 represents_the_all_selected_mask()时将返回true），那么返回0，因为没有明确的大小。 
* [0.x.5]*
   返回一个特定的块是否被这个掩码所选择。如果这个掩码代表了选择[1.x.4]的对象的情况（例如，如果它是使用默认的构造函数创建的，或者是从bool类型的空向量转换而来的），那么无论给定的参数是什么，这个函数都返回true。     [2.x.12] block_index 该函数应返回是否选择该块的索引。如果这个对象代表一个掩码，其中所有的块总是被选中，那么这里允许任何索引。否则，给定的索引需要在零和这个掩码所代表的块数之间。 
* [0.x.6]*
   返回此块掩码是否正好代表[2.x.13]块的掩码。如果它被初始化为一个正好有[2.x.14]的向量（在这种情况下，[2.x.15]必须等于size()的结果），或者它被初始化为一个空向量（或使用默认构造函数），在这种情况下，它可以代表一个有任意数量块的掩码，并且将总是说一个块被选中，这就是真的。 
* [0.x.7]*
   返回这个掩码所选择的区块的数量。    由于空的区块掩码代表一个区块掩码，每个区块都会返回[2.x.16]，这个函数可能不知道区块掩码的真实大小，因此它需要一个参数来表示区块的总数量。    如果该对象已被初始化为一个非空的掩码（即，如果size()函数返回大于0的东西，或者等价地，如果respresent_the_all_selected_mask()返回false），那么该参数可以被省略，而size()的结果被取。 
* [0.x.8]*
   返回第一个被选中的块的索引。该参数存在的原因与n_selected_blocks()函数存在的原因相同。    如果根本没有选择任何块，该函数会抛出一个异常。 
* [0.x.9]*
   如果这个掩码代表一个默认构造的掩码，对应于所有块都被选中的掩码，则返回true。如果为真，那么size()函数将返回0。 
* [0.x.10]*
   返回一个包含由当前对象选择的块和作为参数传递的块的联合体的块掩码。 
* [0.x.11]*
   返回一个块掩码，它只包含那些在当前对象和作为参数传递的对象中都被设置的元素。 
* [0.x.12]*
   返回这个对象和参数是否相同。 
* [0.x.13]*
   返回这个对象和参数是否不相同。 
* [0.x.14]*
   确定此对象的内存消耗（以字节为单位）的估计值。 
* [0.x.15]*
   实际的块掩码。 
* [0.x.16]*
 将块掩码写到输出流中。如果该块掩码代表一个所有块都被选中而没有指定掩码的特定大小，那么它将把字符串[2.x.17]写到流中。否则，它将以[2.x.18]的形式打印块掩码。
* [2.x.19] out 要写入的流。 [2.x.20] mask 要写入的掩码。 [2.x.21] 对第一个参数的引用。

* 
* [0.x.17]

