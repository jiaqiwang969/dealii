include/deal.II-translator/multigrid/mg_transfer.templates_0.txt
[0.x.0]*
     将所有层次的向量调整到正确的大小。 在这里，我们只是计算每一层的自由度数，并[2.x.0]每一层的向量到这个长度。为了与下一个函数兼容，这里加入了target_component，但没有使用。   
* [0.x.1]*
     将所有级别的向量调整到正确的大小。 在这里，我们只是计算每一层的自由度数，并[2.x.1]每一层的向量到这个长度。target_component被交给[2.x.2]见那里的文件。   
* [0.x.2]*
     将所有级别的向量调整到正确的大小。 在这里，我们只是计算每一层的自由度数量，并[2.x.3]每一层的向量到这个长度。   
* [0.x.3]*
     调整所有级别的向量到正确的大小。 这里，我们只是计算每一级的自由度数，并[2.x.4]每一级的向量到这个长度。   
* [0.x.4]

