include/deal.II-translator/lac/sparse_matrix.templates_0.txt
[0.x.0]*
     使用SparseMatrix数据结构执行vmult，但只使用行索引的子区间。        在顺序的情况下，这个函数被调用到所有的行上，在并行的情况下，它可能被调用到一个子区间，由任务调度员决定。   
* [0.x.1]*
     使用SparseMatrix数据结构执行vmult，但只使用子区间的行索引。        在顺序的情况下，这个函数是在所有行上调用的，在并行的情况下，它可以在一个子区间上调用，由任务调度员决定。   
* [0.x.2]*
     使用SparseMatrix数据结构执行vmult，但只使用子区间的行索引。        在顺序情况下，这个函数在所有行上调用，在并行情况下，它可以在一个子区间上调用，由任务调度员决定。   
* [0.x.3]*
     使用SparseMatrix数据结构执行vmult，但只使用子区间的行索引。        在顺序情况下，这个函数在所有行上调用，在并行情况下，它可以在一个子区间上调用，由任务调度员决定。   
* [0.x.4]

